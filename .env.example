# Integrate keycloak or not by adding the keycloak yml file to the COMPOSE_FILE
# COMPOSE_FILE="docker-compose.yml:docker-compose.keycloak.yml"
COMPOSE_FILE="docker-compose.yml:docker-compose.keycloak.yml"